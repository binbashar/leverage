name: Run tests 

on: [pull_request]

defaults:
  run:
    shell: bash

jobs:
  output_flag:
    runs-on: ubuntu-latest
    outputs:
      myflag: ${{ steps.flagstep.outputs.myflag }} 
    steps:
      - uses: actions/checkout@v2
      - id: flagstep
        run: echo "::set-output name=myflag::false"
  
  publish_comment:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
    needs: output_flag
    steps:
      - uses: actions/checkout@v2
      - uses: actions/github-script@v4
        if: ${{ needs.output_flag.outputs.myflag == 'true' }}
        with:
          script: |
            github.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: '🚀🚀'
            })

#  unit_tests:
#    runs-on: ubuntu-latest 
#    steps:
#      - uses: actions/checkout@v2

#      - name: run_unit_tests
#        run: |
#          echo "[INFO] Running unit tests"
#          make test-unit-no-cov
#        shell: bash
#    
#  integration_tests:
#    runs-on: ubuntu-latest
#    steps:
#      - uses: actions/checkout@v2

#      - name: run_integration_tests
#        run: |
#          echo "[INFO] Running integration tests"
#          make test-int
#        shell: bash
